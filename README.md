# mssql-file-uploader
A Python tool for uploading files through MSSQL’s xp_cmdshell using Impacket. Encodes files in Base64, sends them in safe chunks, and generates SQL/PowerShell helpers to decode remotely. Built for pentesters needing reliable file transfer in restricted environments.
